describe('Resolution', () => {
  describe('7.', () => {
    describe(`All conformant DID resolvers MUST implement the DID resolution functions 
      for at least one DID method and MUST be able to return a DID document in at 
      least one conformant representation.`, () => {
      test.todo('untestable');
    });
  });
});
